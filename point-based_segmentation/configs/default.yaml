model:
  local_dir: checkpoints/sam-vit-base

runtime:
  device: auto

input:
  image_path: data/input/sample1.jpg
  resize:
    enabled: true
    size: 1024

prompt:
  points: []
  labels: []

output:
  dir: data/output
  save_mask: true
  save_overlay: true
  save_panel: true

